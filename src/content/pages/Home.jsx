import React from 'react';

const Home = (props) => {
    return(
        <div>
            <h1>Home Stub</h1>
            <button onClick={props.handleLogout}>Logout</button>
        </div>
        
    )
}

export default Home